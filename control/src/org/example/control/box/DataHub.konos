dsl Konos

Datalake(path = "{datalake_path}") as Local

Datamart(type = Graph) graph
	Mounter VaccineMounter as Event
    	Require("example.Vaccine")

Subscriber(event = "example.Vaccine", subscriberId = "Vaccine") vaccineSubscriber
