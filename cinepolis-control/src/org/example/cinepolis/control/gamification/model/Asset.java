package org.example.cinepolis.control.gamification.model;

import io.intino.gamification.graph.model.old.Item;

public class Asset extends Item {

    public Asset(String worldId, String id) {
        super(worldId, id);
    }
}
