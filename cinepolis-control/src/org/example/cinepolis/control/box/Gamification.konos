dsl Konos

Sentinel NewMatch as ClockListener(pattern = "", timeZone = "Atlantic/Canary")
	mean = "Cada semana"

Sentinel PointChecker as ClockListener(pattern = "", timeZone = "Atlantic/Canary")
	mean = "Cada día"

Sentinel MissionEndChecker as ClockListener(pattern = "", timeZone = "Atlantic/Canary")
	mean = "Cada hora"

Sentinel CinemaWeeklyVisit as ClockListener(pattern = "", timeZone = "Atlantic/Canary")
	mean = "Cada semana"

//Falta añadir el generador de misiones
